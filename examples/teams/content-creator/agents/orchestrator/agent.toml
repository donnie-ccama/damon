[agent]
name = "Orchestrator"
role = "Single-session producer: brief, research, draft, and final essay with one approval gate"
runtime = "claude"
default_model = "claude"

[repo]
source = "new"
branch = "agent/orchestrator"
